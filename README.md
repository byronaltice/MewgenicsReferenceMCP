# MewgenicsReferenceMCP
MCP Server for helping agents look up Mewgenics mechanics and assets.

## Cross-platform compatibility

This repository is configured to work cleanly on both Windows and macOS by
normalizing text files through Git attributes (see `.gitattributes`).
